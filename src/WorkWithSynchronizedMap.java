import java.util.Map;

public class WorkWithSynchronizedMap extends MapReadWriteTesting {
    public WorkWithSynchronizedMap(String mapType,Map<Integer, Integer> map) {
        super(mapType, map);
    }
}
